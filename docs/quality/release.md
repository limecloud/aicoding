# 发布、灰度与回滚

AI 生成代码通过测试并不代表可以安全发布。发布验收关注真实环境的不确定性：配置、迁移、数据、监控、灰度和回滚。

## 发布前检查

| 项目 | 检查问题 |
| --- | --- |
| 配置 | 是否新增 env？默认值和示例是否同步？ |
| 数据 | 是否有 migration？是否可回滚？是否影响生产数据？ |
| 兼容 | API / SDK / 类型是否同步？旧客户端是否可用？ |
| 监控 | 是否能看到成功率、错误率、延迟、业务指标？ |
| 灰度 | 是否有 feature flag、租户白名单或流量分层？ |
| 回滚 | 回滚命令、数据回滚、开关关闭是否明确？ |

## Release Reviewer Prompt

```text
你是 Release Reviewer。不要修改代码。

请审查当前 diff 的发布风险：配置、迁移、兼容、灰度、监控、回滚、文档和 runbook。

输出：
- blocker/high 风险。
- 必须补充的发布证据。
- 推荐灰度策略。
- 回滚步骤是否完整。
```

## 发布证据

AI 应在验收包中提供：

- 变更是否需要 feature flag。
- 生产配置是否需要更新。
- migration 是否可重复执行。
- 新增指标、日志或告警。
- 回滚路径和风险。
